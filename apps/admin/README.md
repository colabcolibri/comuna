# admin

Next de ops na porta 3015. Sem chrome Stitch. Sessão `ops_token`.

```bash
pnpm dev:admin
# sessão só de leitura, login sem OTP:
pnpm dev:admin:demo
```

Abrir `http://localhost:3015`. Seed: `INITIAL_SUPER_ADMIN_EMAIL`. Demo: `pnpm dev:admin:demo` — não misturar com outro `next dev` em `apps/admin`.
